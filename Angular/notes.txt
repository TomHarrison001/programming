requires node.js

node --version
npm install -g @angular/cli
ng version
ng new projectName

compile:
ng serve

npm install -g typescript
tsc --version

tsc main.ts | node main.js (compiles main.ts to main.js and executes)

new component:
ng g c componentName

new service:
ng g s serviceName
